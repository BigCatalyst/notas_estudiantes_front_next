// import { withAuth } from "@/components/withAuth";

const Dashboard = () => {
    return <div>Dashboard</div>;
  };
  
  export default Dashboard;
  // export default withAuth(Dashboard, ['admin', 'editor']);