import { UpdateEvent } from "@/components/dashboard/school_event/UpdateEvent";
import React from "react";

const Update = () => {
  return (
    <div>
      <UpdateEvent />
    </div>
  );
};

export default Update;
